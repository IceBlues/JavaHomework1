class EmployeeManager extends Employee {
    /**
     * Construct a Manager Object.
     *
     * @param name The Employee name
     * @param id The EmployeeID that was the unique ID of the Employee
     * */
    EmployeeManager(String name, String id) {
        super(name, id);
    }
}
